var { sendEmbed, client, data } = require("../bot.js")

module.exports = {
    name: "statistics",
    description: "Puts statistics at the top of your server",
    required: false,
    events: [],
    run: (name, data) => { }
}